# Kutty SIH Environment Configuration
PORT=5000
NODE_ENV=production
JWT_SECRET=kutty-sih-production-jwt-secret-key-2025

# Gemini API Key (Backend only, never exposed to frontend)
# If left blank or unset, system uses built-in heuristic qualitative analysis
GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE
